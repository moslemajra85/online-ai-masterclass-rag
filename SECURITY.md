# Security

Do not report suspected vulnerabilities in public issues. Use GitHub's private
vulnerability reporting feature after the repository is published.

Never commit provider credentials or participant information. Copy `.env.example`
to `.env` locally, keep `.env` ignored, and rotate a credential immediately if it is
accidentally exposed.
